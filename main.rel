using <Logger>

public main() {
    define password = Logger.inputRead("Password? ")
    
    if (password == "a") correct()
    #if (password != "a") wrong()
    
    void correct() {
        Logger.println("Welcome!")
    }(correct)
    
    void wrong() {
        Logger.println("Incorrect password.")
    }(wrong)
}(main)