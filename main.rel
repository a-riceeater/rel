using <Logger>

public main() {
    define password = Logger.inputRead("Password? ")
    
    if (password == "shin") correct()
    if (password != "shin") wrong()
    
    void correct() {
        Logger.println("Welcome!")
    }(correct)
    
    void wrong() {
        Logger.println("Incorrect password.")
    }(wrong)
}(main)