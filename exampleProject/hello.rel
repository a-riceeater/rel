using <Logger>

public hello() {
    //define name = Logger.inputRead("Name: ")
    welcome(name="asd", a="a")

    void welcome() {
        Logger.println("Hello " + Params.a);
    }(welcome)
}(hello)